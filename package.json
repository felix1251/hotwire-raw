{
  "name": "rust-on-rails",
  "version": "1.0.0",
  "description": "Hotwire Example",
  "scripts": {
    "build": "esbuild javascript/*.* --bundle --minify --outdir=assets/builds --public-path=/assets"
  },
  "author": "felixabacajen17@gmail.com",
  "license": "ISC",
  "dependencies": {
    "@hotwired/stimulus": "^3.2.2",
    "@hotwired/turbo": "^8.0.0-beta.1",
    "esbuild": "^0.19.9"
  }
}
